# 수문재국어전문학원 학생 관리 시스템 사용설명서

---

## 목차

1. [시작하기 — 로그인](#1-시작하기--로그인)
2. [역할별 화면 구성](#2-역할별-화면-구성)
3. [대시보드](#3-대시보드)
4. [학생 관리](#4-학생-관리)
5. [출결 관리](#5-출결-관리)
6. [성적 관리](#6-성적-관리)
7. [영상 관리](#7-영상-관리)
8. [테스트](#8-테스트)
9. [과제](#9-과제)
10. [Q&A 게시판](#10-qa-게시판)
11. [공지사항](#11-공지사항)
12. [주간 리포트](#12-주간-리포트)
13. [진도 리포트](#13-진도-리포트)
14. [계정 관리 (관리자 전용)](#14-계정-관리-관리자-전용)
15. [비밀번호 변경](#15-비밀번호-변경)
16. [자주 묻는 질문 (FAQ)](#16-자주-묻는-질문-faq)

---

## 1. 시작하기 — 로그인

### 접속 주소
웹 브라우저에서 배포된 URL로 접속합니다.

### 로그인 방법
1. 아이디(username)와 비밀번호를 입력합니다.
2. **로그인** 버튼을 누릅니다.

### 역할별 계정
| 역할 | 아이디 형식 | 초기 비밀번호 | 권장 기기 |
|------|------------|--------------|-----------|
| 관리자 | 관리자가 설정 | 관리자가 설정 | PC + 모바일 |
| 교사 | 관리자가 생성 | 관리자가 설정 | PC + 모바일 |
| 학생 | 관리자가 생성 | `0000` (첫 로그인 후 변경 필요) | 모바일 |

> **학생 첫 로그인 시** 반드시 비밀번호를 변경해야 합니다. 변경하지 않으면 이후 화면으로 이동할 수 없습니다.

### 교사가 볼 수 있는 범위
교사에게는 **자기가 담당하는 반과 그 반 학생만** 보입니다. 학생 목록, 출결, 성적,
리포트, 영상, 테스트, Q&A, 과제 제출 현황 모두 담당 반 기준으로 걸러집니다.
관리자는 모든 반을 봅니다.

- 담당 반은 **학생 관리 → 반 관리**에서 관리자가 지정합니다
- 배정 전 교사에게는 "담당 반이 없습니다" 안내가 뜹니다 — 화면이 비어 보이면 반 배정부터 확인하세요
- 대상 반을 지정하지 않은 **학원 전체 공지**는 담당 반과 상관없이 모두에게 보입니다

---

## 2. 역할별 화면 구성

### 관리자 / 교사 (PC)
- 왼쪽에 **사이드바** 메뉴가 고정 표시됩니다.
- 로고를 클릭하면 대시보드로 이동합니다.

### 관리자 / 교사 (모바일)
- 상단 헤더 좌측 **☰ 버튼**을 눌러 메뉴를 열 수 있습니다.
- 메뉴 항목을 선택하거나 바깥 영역을 누르면 메뉴가 닫힙니다.

### 학생 (모바일)
- 상단에 로고, 하단에 **탭 네비게이션**이 표시됩니다.
- 홈 / 출결 / 성적 / 영상 / 테스트 / 과제 / Q&A / 공지 탭으로 이동합니다.

---

## 3. 대시보드

### 관리자 / 교사 화면
- **전체 학생 수** — 담당 반 학생 수 표시
- **과제 미제출** — 마감이 지난 과제를 안 낸 학생 수 (빨간색 강조, 클릭 시 과제 페이지로 이동)
  - **이번 주** 과제만 셉니다. 지난 주에 빼먹은 것은 여기 안 나옵니다
  - **마감이 지난 회차만** 셉니다. 아직 마감 전인 요일은 세지 않습니다 (단, 오늘 마감인 과제는 포함됩니다)
  - 한 학생이 여러 날 빼먹어도 **1명**으로 셉니다 — "몇 명을 불러야 하는가"를 보는 숫자입니다
  - 담당 반 학생만 셉니다 (관리자는 전체)
- **미답변 Q&A** — 답변 대기 중인 질문 수 (노란색 강조, 클릭 시 Q&A 페이지로 이동)
- **오늘 출결 현황** — 반별 출석/결석/지각 현황 (클릭 시 출결 관리로 이동)
- **최근 테스트** — 최근 테스트 3개와 평균 점수
- **최근 공지사항** — 최근 공지 2개

### 학생 화면
- **이번 주 과제** — 요일별 제출 여부. 마감이 있어 가장 급하므로 맨 위에 표시됩니다
- **이번 달 출결** — 출석률(%) + 출석/결석/지각 건수
- **최근 테스트 결과** — 채점 완료된 테스트 결과
- **최근 성적** — 교사가 입력한 성적 3건
- **Q&A 답변** — 최근 답변 받은 질문
- **공지사항** — 나의 반에 해당하는 최근 공지

---

## 4. 학생 관리

> 관리자 / 교사만 접근 가능합니다.

### 학생 목록
- 반 탭을 눌러 반별로 필터링할 수 있습니다.
- **검색창**에 이름 일부나 전화번호 뒷자리를 넣으면 그 학생만 남습니다 (학부모 번호로도 찾습니다).
  검색 중에는 순서 바꾸기(드래그)가 잠깁니다 — 일부만 보이는 상태에서 순서를 바꾸면 뒤엉키기 때문입니다.
- 학생 카드를 클릭하면 상세 정보(이름, 연락처, 학부모 연락처, 입학일)를 확인할 수 있습니다.

### 학생 추가
1. 우측 상단 **+ 학생 추가** 버튼 클릭
2. 이름, 반, 연락처, 학부모 연락처, 입학일 입력
3. **저장** 클릭

### 학생 수정
1. 학생 카드 클릭
2. 상세 모달에서 **수정** 버튼 클릭
3. 정보 변경 후 **저장** 클릭

### 학생 삭제
- 학생 카드에서 **삭제** 버튼 클릭 (관리자 또는 본인 담당 반만 가능)
- 여러 학생을 체크박스로 선택 후 **일괄 삭제** 가능

### 엑셀 일괄 업로드
1. **엑셀 업로드** 버튼 클릭
2. 아래 형식의 엑셀 파일(.xlsx) 선택

| 성명 (또는 이름) | 반명 (또는 반) | 학생핸드폰 (또는 연락처) | 부모핸드폰 (또는 학부모연락처) |
|----------------|--------------|----------------------|---------------------------|
| 홍길동 | 고1 수능반 | 010-1234-5678 | 010-8765-4321 |

> 반 이름은 시스템에 등록된 반 이름과 정확히 일치해야 합니다.

### 반 관리
- 상단 탭에서 **반 관리** 클릭
- 반 추가, 수정, 삭제, 순서 변경 가능
- 반 추가/수정 창의 **담당 교사** 항목에서 교사를 배정합니다 (관리자만 가능)
- 반 카드에 `담당 ○○○`으로 현재 배정된 교사가 표시됩니다
- 담당 교사가 **미지정**이면 그 반은 교사 화면(대시보드·출결)에 나타나지 않습니다

---

## 5. 출결 관리

### 수업 출결 (교사 / 관리자)
1. 날짜를 선택합니다 (기본값: 오늘)
2. 반 탭을 선택합니다.
3. 학생 이름 옆 상태 버튼을 클릭해 상태를 변경합니다.
   - `미기록` → `출석` → `결석` → `지각` → `미기록` 순서로 순환됩니다.

### 클리닉 출결 (교사 / 관리자)
1. 날짜를 선택합니다.
2. 하단 검색창에 학생 이름을 입력합니다.
3. **+ 추가** 버튼을 눌러 참석 처리합니다.
4. 참석 취소는 참석자 목록에서 **참석 ✓** 버튼을 클릭합니다.

### 학생 출석 체크 (학원 WiFi 필요)
1. 학원 WiFi에 연결된 상태에서 앱에 접속합니다.
2. 대시보드 또는 출결 페이지에서 **출석 체크** 버튼을 누릅니다.
3. 학원 WiFi가 감지되면 자동으로 출석이 기록됩니다.
4. 학원 외부에서는 출석 체크가 불가능합니다 (대리출석 방지).

> 이미 출석 체크한 날에는 버튼이 비활성화됩니다.
>
> 학원 지점이 여러 곳이어도 됩니다. 각 지점의 공인 IP를 배포 환경변수 `ACADEMY_IPS`에
> 쉼표로 이어 적어두면(예: `1.2.3.4,5.6.7.8`) 그중 어느 지점에서 접속해도 출석이 인정됩니다.
> 지점이 늘어날 때는 값만 덧붙이면 되고 코드는 고치지 않습니다.

---

## 6. 성적 관리

> 관리자 / 교사만 입력 가능하며, 학생은 본인 성적만 조회할 수 있습니다.

### 성적 입력 (교사 / 관리자)
1. 반 탭을 선택합니다.
2. 학생 이름 옆 **+ 성적 추가** 버튼 클릭
3. 유형(주간테스트 / 내신), 날짜, 과목, 파트, 점수, 만점 입력
4. **저장** 클릭

### 성적 내역 조회
- 학생 이름을 클릭하면 해당 학생의 전체 성적 내역 모달이 열립니다.
- 여러 건의 성적이 있으면 카드에 **(+N)** 배지가 표시됩니다.

### 성적 삭제
- 성적 내역 모달에서 각 성적 항목의 **삭제** 버튼 클릭

### 학생 화면
- 본인의 성적만 표시됩니다.
- 점수와 과목/파트 정보를 확인할 수 있습니다.
- 등급은 표시되지 않습니다.

---

## 7. 영상 관리

### 영상 등록 (교사 / 관리자)
1. 우측 상단 **+ 영상 등록** 버튼 클릭
2. 제목, YouTube URL, 대상 반 선택
3. **등록** 클릭 (썸네일이 자동으로 추출됩니다)

> YouTube URL 예시: `https://youtu.be/abc123` 또는 `https://www.youtube.com/watch?v=abc123`

### 영상 시청
- 영상 카드를 클릭하면 앱 내에서 바로 재생됩니다.
- 학생은 본인 반에 등록된 영상만 볼 수 있습니다.

### 영상 삭제 (교사 / 관리자)
- 영상 카드 우측 상단 **✕** 버튼 클릭

### 댓글 (질문)
- 학생은 영상 하단에 익명으로 질문을 남길 수 있습니다.
- 교사/관리자 화면에서는 질문자의 실명이 표시됩니다.
- 교사/관리자는 댓글에 답글을 달 수 있습니다.

---

## 8. 테스트

### 테스트 생성 (교사 / 관리자)
1. 우측 상단 **+ 테스트 만들기** 버튼 클릭
2. 제목, 대상 반, 날짜, 시간 제한 설정
3. **객관식 문항 수**와 **총점**을 입력합니다
   - 문항 수를 넣으면 정답 표가 바로 나옵니다 (과제 출제와 같은 방식)
   - 배점은 총점을 문항 수로 나눠 자동으로 정해집니다
   - 나누어떨어지지 않으면 **가까운 총점을 알려줍니다** (예: 3문항이면 99점 또는 102점).
     그대로 두어도 되며, 이때는 뒤쪽 문항이 0.1점 적어지고 합계는 총점과 정확히 맞습니다
4. 정답 표에서 각 문항의 정답을 지정합니다
   - 선지를 누르거나, 표를 클릭한 뒤 **숫자키 1~5**로 넣어도 됩니다
   - 문항 수를 줄이면 사라진 문항의 정답도 함께 지워집니다
5. 주관식이 있으면 **+ 주관식** 버튼으로 객관식 뒤에 붙입니다 (교사가 직접 채점)
6. **저장** 클릭

> 정답을 지정하지 않은 문항이 있으면 저장 버튼이 꺼지고, 몇 번인지 아래에 알려줍니다.

> **정답이 여러 개인 문항**
>
> 선지를 여러 개 눌러 두면 그 문항의 정답이 여러 개가 됩니다(「모두 고르시오」형).
> 이미 켜진 선지를 다시 누르면 꺼집니다.
>
> 학생도 같은 방식으로 여러 개를 체크해 제출합니다. **정답을 전부 맞혀야 정답 처리**되며,
> 하나라도 빠뜨리거나 더 고르면 오답입니다.

### 테스트 진행
| 상태 | 설명 |
|------|------|
| 준비중 | 학생에게 미노출, 교사가 시작 버튼 누를 때까지 대기 |
| 진행중 | 학생이 답안 입력 가능, 시간 제한 카운트다운 표시 |
| 종료 | 더 이상 제출 불가, 교사가 채점 진행 |

1. 교사/관리자가 **시작** 버튼을 누르면 학생 화면에 테스트가 활성화됩니다.
2. 시간 제한이 끝나면 자동으로 제출됩니다.
3. 테스트가 끝나면 **종료** 버튼을 누릅니다.

### 채점 (교사 / 관리자)
1. 종료된 테스트에서 **제출 현황** 클릭
2. 학생 이름 옆 **채점** 버튼 클릭
3. 주관식 문항은 직접 점수를 입력, 객관식은 자동 채점
4. **저장** 클릭

### 학생 화면
- 진행 중인 테스트만 표시됩니다.
- 답안 입력 후 **제출** 버튼을 누릅니다.
- 채점이 완료되면 **결과 보기**에서 점수를 확인할 수 있습니다.

---

## 9. 과제

주간 단위로 요일별 과제를 내고 학생 제출을 받는 기능입니다.
테스트와 달리 **오프라인 문제지를 풀고 답만 입력**하는 방식입니다.

화면 위쪽에 **내신과제 / 정시과제** 탭이 있습니다. 두 종류는 완전히 따로 운영됩니다.

| 종류 | 배정 기준 | 예 |
|------|-----------|-----|
| 내신과제 | **반** | 동화리 고2A반 등 |
| 정시과제 | **정시 레벨** | 1레벨 / 2레벨 / 3레벨 |

> 내신과제는 **반별로** 나갑니다. 반마다 진도가 다르기 때문입니다. 정시과제는 반과 무관하게 레벨로 묶입니다.
> **반이 배정되지 않은 학생은 내신과제를 받지 못합니다** — 학생 관리에서 반을 지정해 주세요.
> 정시 레벨이 배정되지 않은 학생은 정시과제 화면에 안내 문구만 보입니다 — 마찬가지로 학생 관리에서 지정합니다.
>
> 반별로 바뀌기 전에 학년 단위로 낸 과제는 학생 화면과 주간 리포트에 그대로 남습니다.
> 다만 교사의 제출 현황·리포트 탭은 반 기준이라 거기에는 나타나지 않습니다.

### 주간 과제 출제 (교사 / 관리자)

1. 우측 상단 **+ 주간 과제** 버튼 클릭
2. **세트 제목**(예: 8월 2주차), **반 또는 정시 레벨**, **주 시작(월요일)** 을 정합니다
   - 내신은 담당 반만 고를 수 있습니다. 반마다 따로 출제해야 합니다
3. 요일 탭(월~토)에서 과제를 낼 요일을 고르고 **「○요일 과제 사용」** 체크
4. **문항 수**를 입력하면 정답 입력표가 나옵니다
5. 선지를 눌러 정답을 지정합니다
6. 필요하면 **해설 영상(YouTube 링크)** 과 **해설 파일**을 올립니다
7. **주간 과제 저장** 클릭

제목과 사용 요일이 있고, 사용한 요일의 정답이 **모두** 입력돼야 저장 버튼이 켜집니다.
여러 요일을 켰다면 **켠 요일 전부**를 채워야 합니다 — 덜 채운 요일은 탭에 빨간 `!` 로 표시되고,
저장 버튼 위에 어느 요일이 남았는지 나옵니다.

> **정답이 여러 개인 문항**
>
> 선지를 여러 개 눌러 두면 그 문항의 정답이 여러 개가 됩니다(「모두 고르시오」형).
> 이미 켜진 선지를 다시 누르면 꺼집니다.
>
> 학생도 같은 방식으로 여러 개를 체크해 제출합니다. **정답을 전부 맞혀야 정답 처리**되며,
> 하나라도 빠뜨리거나 더 고르면 오답입니다.
>
> **정답을 고칠 때는 두 번 누릅니다** — 옛 정답을 눌러 끄고, 새 정답을 눌러 켭니다.
>
> 키보드로 입력하면 빠릅니다. 숫자 **1~5** 로 선지를 켜고 끄고, **Enter** 로 다음 문항으로 넘어갑니다.

### 이미 낸 과제 수정하기

목록에서 세트를 골라 수정할 수 있습니다. 저장하기 전에 **이미 제출한 학생에게 어떤 영향이 가는지** 화면에 미리 나옵니다.

| 수정 내용 | 결과 |
|-----------|------|
| 정답 변경 | 제출한 학생들의 채점 결과가 다시 계산됩니다 |
| 문항 수 증가 | 늘어난 문항이 기존 제출자에게 오답 처리됩니다 |
| 문항 수 감소 | 만점이 줄어듭니다 |
| 요일 사용 해제 | **그 요일의 제출 기록이 영구 삭제됩니다 — 되돌릴 수 없습니다** |

### 해설 파일 관리

- 파일을 올리면 파일명과 **삭제** 버튼이 보입니다.
- 삭제는 **저장을 눌러야 반영**됩니다. 저장하지 않고 나가면 학생 화면의 해설 링크는 그대로 살아 있습니다.

### 제출 현황 (교사 / 관리자)

**제출 현황** 버튼을 누르면 요일별로 누가 냈는지 볼 수 있습니다.
학생 이름을 누르면 그 학생의 문항별 정답·오답이 나옵니다.

### 잘못 낸 제출 되돌리기

학생은 한 번 제출하면 스스로 못 고칩니다. 실수로 낸 경우 교사가 취소해 줄 수 있습니다.

1. **제출 현황** → 요일 → 학생 이름
2. 아래쪽 **제출 취소** 클릭
3. 확인창에서 승인

취소하면 그 학생의 답안이 **완전히 삭제되고** 다시 처음부터 풀어 낼 수 있습니다.
마감이 지난 뒤에 다시 내면 **지각제출**로 표시됩니다.

> **되돌릴 수 없습니다.** 취소한 답안은 복구할 방법이 없습니다.
>
> 취소할 수 있는 사람은 **그 과제를 낸 교사와 관리자**뿐입니다. 다른 교사에게는 버튼이 보이지 않습니다.

### 리포트 (교사 / 관리자)

**리포트** 버튼을 누르면 한 달 동안 반·레벨별 학생들의 **제출률과 정답률**이 나옵니다.

- 제출률이 **70% 미만**이면 눈에 띄게 표시됩니다.
- 정답률은 **제출한 회차의 문항만** 기준으로 계산합니다. 안 낸 회차를 섞으면 성실도와 실력이 뒤엉켜 어느 쪽이 문제인지 알 수 없기 때문입니다.

### 과제 제출 (학생)

1. 사이드바(모바일은 하단 바) → **과제**
2. 내신 / 정시 탭을 고릅니다
3. 요일 카드를 눌러 들어갑니다
4. 선지를 눌러 답을 입력합니다 — **모든 문항을 채워야** 제출 버튼이 켜집니다
5. **제출하기** 클릭

> **제출한 뒤에는 답을 수정할 수 없습니다.** 제출 전에 반드시 확인하세요.
> 마감일이 지나 제출하면 **지각제출**로 표시됩니다.

제출하고 나면 같은 화면에서 **점수와 문항별 정답·오답**, 그리고 교사가 올린 **해설 영상·파일**을 볼 수 있습니다.

| 뱃지 | 뜻 |
|------|-----|
| 미제출 | 아직 답을 내지 않음 |
| 제출완료 | 마감 안에 제출함 |
| 지각제출 | 마감이 지난 뒤 제출함 |

---

## 10. Q&A 게시판

질문에는 **말머리** 하나만 붙입니다. 어느 테스트의 몇 번 문항인지까지 고를 필요가 없습니다.

| 말머리 | 쓰는 경우 |
|--------|-----------|
| 내신과제 | 내신과제 문항·해설·마감 관련 |
| 정시과제 | 정시과제 문항·해설·마감 관련 |
| 테스트 | 주간테스트 문항·채점 관련 |
| 기타 | 위 셋에 안 맞는 것 (수업, 일정 문의 등) |

### 질문 등록 (학생)
1. 우측 상단 **+ 질문하기** 버튼 클릭
2. **말머리**를 고릅니다 (기본값: 내신과제)
3. 질문 내용 입력
4. 필요하면 **사진 추가**로 사진을 붙입니다 (최대 3장, 선택)
5. **질문 등록** 클릭

> 질문은 **담당 선생님과 관리자만** 볼 수 있습니다. 다른 학생에게는 보이지 않습니다.
>
> 테스트가 없어도 질문할 수 있습니다. 예전에는 종료된 테스트를 먼저 골라야 해서
> 과제 관련 질문을 낼 방법이 없었습니다.

#### 사진 첨부
- 폰에서 **사진 추가**를 누르면 카메라와 앨범 중에 고를 수 있습니다.
- 한 질문에 **최대 3장**입니다. 3장이 차면 추가 버튼이 사라집니다.
- 붙인 사진 오른쪽 위 **×**를 누르면 뺍니다.
- 올릴 때 자동으로 크기를 줄입니다. 폰 사진 한 장이 3~5MB인데 그대로 올리면
  느리고 데이터도 많이 씁니다. 줄여도 답안지 글씨는 읽힙니다.
- 사진이 붙은 질문은 목록에 📷 표시와 장수가 나옵니다.

> 사진이 하나라도 안 올라가면 질문을 등록하지 않고 작성 화면에 남습니다.
> "사진 보고 답해 주세요"라고 쓴 질문만 올라가면 답을 받을 수 없기 때문입니다.

### 답변 등록 (교사 / 관리자)
1. 질문 카드를 클릭합니다.
2. 사진이 붙어 있으면 질문 내용 아래에 함께 보입니다. 사진을 누르면 새 탭에서 크게 볼 수 있습니다.
3. 하단 답변 입력창에 내용을 작성합니다.
4. **답변 등록** 버튼 클릭

### 필터
- 상단 탭에서 **말머리별로** 질문을 걸러 볼 수 있습니다.
- 미답변 건수는 상단에 빨간 숫자로 표시됩니다.

### 누구의 질문이 보이나
- **관리자** — 전체
- **교사** — 담당 반 학생의 질문
- **학생** — **본인이 쓴 질문만**

> Q&A는 1:1 상담입니다. 같은 반이어도 다른 학생의 질문은 보이지 않습니다.
> 질문에 답안지 사진이 붙기 때문입니다. 사진에 손글씨나 이름이 찍혀 있으면
> 작성자를 "익명"으로 가려도 누구인지 드러납니다.

> 반이 배정되지 않은 학생의 질문은 관리자에게만 보입니다. 학생 관리에서 반을 지정해 주세요.

---

## 11. 공지사항

### 공지 작성 (교사 / 관리자)
1. 우측 상단 **+ 공지 작성** 버튼 클릭
2. 제목과 내용 입력
3. 대상 반 선택 (전체 또는 특정 반)
4. **저장** 클릭

### 공지 삭제
- 본인이 작성한 공지 또는 관리자는 삭제 가능
- 공지 카드 우측 하단 **삭제** 버튼 클릭

### 학생 화면
- 본인 반에 해당하는 공지만 표시됩니다.

---

## 12. 주간 리포트

> 교사 / 관리자만 접근 가능합니다. 학생 화면에는 표시되지 않습니다.

사이드바 → 소통 → 주간 리포트

한 반의 한 주를 표 하나로 봅니다. 출석·주간테스트·내신과제·정시과제가 학생별로 나옵니다.

- 위쪽 `◀ ▶`로 지난 주를 볼 수 있습니다
- ⚠ 표시는 결석이 있거나, 시험을 안 봤거나, 과제 제출률이 70% 미만인 학생입니다.
  이런 학생이 표 위쪽에 먼저 나옵니다
- `-`는 "기록이 없음"입니다. 0점이나 0%와 다릅니다
- `채점중`은 주관식 채점이 아직 안 끝난 시험입니다
- 학생 이름을 누르면 요일별 출결과 시험별 점수를 볼 수 있고, 상담용 코멘트를 남길 수 있습니다

---

## 13. 진도 리포트

> 교사 / 관리자만 접근 가능합니다. 학생 화면에는 표시되지 않습니다.

### 리포트 작성
1. 우측 상단 **+ 리포트 작성** 버튼 클릭
2. 반, 날짜, 과목 선택
3. 진도 내용 입력 (오늘 수업한 내용)
4. 과제 내용 입력 (선택사항)
5. 학생별 과제 수행 여부 체크 (선택사항)
6. **리포트 저장** 클릭

### 과제 수행 현황 업데이트
1. 리포트 카드를 클릭합니다.
2. 학생 이름을 클릭하면 과제 완료/미완료가 토글됩니다.

### 리포트 삭제
- 본인이 작성한 리포트 또는 관리자는 삭제 가능
- 리포트 카드 우측 하단 **삭제** 버튼 클릭

---

## 14. 계정 관리 (관리자 전용)

> 사이드바 하단 **계정 관리** 메뉴 (관리자만 보임)

### 교사 계정 생성
1. **+ 교사 계정 추가** 버튼 클릭
2. 이름, 아이디(username), 초기 비밀번호 입력
3. **생성** 클릭

### 학생 계정 생성
1. 학생 관리 페이지에서 학생 카드 클릭
2. 상세 모달 하단 **계정 생성** 버튼 클릭
3. 아이디(username) 입력 (초기 비밀번호는 `0000`)
4. 학생이 첫 로그인 시 비밀번호 강제 변경

### 계정 삭제
- 교사 계정 목록에서 **삭제** 버튼 클릭

---

## 15. 비밀번호 변경

### 방법 1 — 사이드바에서 (교사 / 관리자)
- 사이드바 하단 **비밀번호 변경** 클릭

### 방법 2 — 헤더 아이콘에서 (학생)
- 헤더 우측 열쇠 아이콘(🔑) 클릭

### 변경 절차
1. 현재 비밀번호 입력
2. 새 비밀번호 입력
3. 새 비밀번호 확인 입력
4. **변경** 버튼 클릭

> 학생 계정은 첫 로그인 시 비밀번호 변경이 강제됩니다.

---

## 16. 자주 묻는 질문 (FAQ)

**Q. 출석 체크가 안 됩니다.**
> 학원 WiFi에 연결되어 있는지 확인해주세요. 학원 외부 네트워크(LTE, 다른 WiFi)에서는 출석 체크가 불가능합니다.

**Q. 학생을 삭제했는데 새로고침하면 다시 나타납니다.**
> 브라우저 캐시 문제일 수 있습니다. 강력 새로고침(Ctrl+Shift+R 또는 Cmd+Shift+R)을 해보세요. 문제가 지속되면 관리자에게 문의하세요.

**Q. 엑셀 업로드 시 반이 "미배정"으로 뜹니다.**
> 엑셀의 반 이름이 시스템에 등록된 반 이름과 정확히 일치해야 합니다. 반 관리 탭에서 반 이름을 확인한 후 엑셀을 수정해주세요.

**Q. 테스트 채점이 저장되지 않습니다.**
> 채점 후 반드시 **저장** 버튼을 눌러야 합니다. 저장 없이 다른 페이지로 이동하면 채점 내용이 사라집니다.

**Q. 비밀번호를 잊어버렸습니다.**
> 관리자에게 문의하면 계정을 삭제 후 재생성할 수 있습니다. 비밀번호 초기화 기능은 추후 추가 예정입니다.

**Q. 학생이 Q&A 질문을 했는데 안 보입니다.**
> 그 학생이 **내 담당 반**인지 확인해주세요. 질문은 작성한 학생 기준으로 보이며, 반이 배정되지 않은 학생의 질문은 관리자에게만 보입니다.

**Q. 학생 화면에 과제가 안 보입니다.**
> 세 가지를 확인하세요. ① 과제의 **주 시작(월요일)** 이 이번 주로 되어 있는지 ② 과제의 **학년·정시 레벨**이 그 학생과 맞는지 ③ 정시과제라면 학생에게 **정시 레벨이 배정**되어 있는지. 과제는 반이 아니라 학년·레벨로 배정되므로, 같은 반이어도 학년이 다르면 다른 과제를 받습니다.

**Q. 학생이 과제를 잘못 제출했습니다. 되돌릴 수 있나요?**
> 교사가 취소해 줄 수 있습니다. **제출 현황** → 요일 → 학생 이름 → **제출 취소**. 취소하면 답안이 삭제되고 학생이 다시 풀어 낼 수 있습니다. 단 **그 과제를 낸 교사와 관리자**만 가능하고, 한 번 취소한 답안은 복구되지 않습니다.

**Q. 정답을 ③으로 바꿨는데 학생들이 다 틀렸습니다.**
> 선지 버튼이 **토글**이라 옛 정답이 켜진 채로 남았을 수 있습니다(예: `①③`). 정답을 고칠 때는 **옛 정답을 눌러 끄고, 새 정답을 눌러 켜야** 합니다. 출제 화면에서 진하게 표시된 선지가 지금의 정답입니다.

**Q. 과제 저장 버튼이 안 눌립니다.**
> **「사용」 체크한 모든 요일**의 정답이 다 입력돼야 켜집니다. 지금 보고 있는 요일이 다 찼어도, 다른 요일을 켜 놓고 비워 뒀다면 막힙니다. 버튼 바로 위에 남은 요일이 글로 나오고, 덜 채운 요일 탭에는 빨간 `!` 가 붙으니 그 탭으로 가서 채우세요. 「사용」만 켜고 문항 수를 안 넣은 요일이 가장 흔한 원인입니다.

---

*수문재국어전문학원 학생 관리 시스템 — 문의: 관리자에게 연락하세요.*
